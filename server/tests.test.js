import { expect } from 'chai';

describe('Server tests are working', () => {
  it('does math', () => {
    expect(1 + 1).to.equal(2);
  });

  it('does more math', () => {
    expect(1 - 1).to.equal(0);
  });

  it('does even more math', () => {
    expect(1 - 2).to.equal(-1);
  });
});
